function setup() {
    createCanvas(800, 600);
    background(10, 200, 200);
}

function draw() {
    // console.log and print() spit out information to the browser's console
    // mosueX and mosueY below report theposition of the mouse in the canvas
    console.log("mouse X position " + mouseX + " mouse Y position " + mouseY);
}